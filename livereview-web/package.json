{
  "name": "livereview-web",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Standalone HTML/JS player for the LiveReview launch film — real animation, not stills.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "devDependencies": { "vite": "^5.4.10" }
}
